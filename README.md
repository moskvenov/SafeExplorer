1. 📁 Расширенный файловый менеджер
Интуитивная навигация: Классический двухпанельный интерфейс для удобного перемещения по директориям.

Полный цикл операций: Создание, переименование, копирование, перемещение и удаление файлов и папок.

Управление метаданными: Просмотр и ручное изменение даты создания/модификации файлов (атрибутов atime, mtime, ctime).

Контекстное меню: Быстрый доступ к операциям создания файлов и папок.

2. 🛡️ Системный блокировщик трафика (через Hosts)
Графический интерфейс для файла hosts: Удобное управление блокировкой доменов без редактирования системного файла вручную.

Предустановленный список ресурсов: База из частых сайтов для администрирования, мониторинга и безопасности (NirSoft, инструменты системного анализа и др.) с функцией быстрого добавления в чёрный список.

Принцип действия: Блокировка нежелательных ресурсов (реклама, трекеры, опасные сайты) путём перенаправления запросов на 127.0.0.1 (локальный хост).

Управление списками: Возможность просмотра, добавления и удаления заблокированных адресов.

3. ✏️ Продвинутый текстовый редактор с "стелс-режимом"
Встроенный редактор кода: Открытие и редактирование текстовых файлов (.txt, .py, .cfg, .log и др.).

Ключевая уникальная функция: «Заморозка даты» файла. После сохранения изменений пользователь может оставить исходную дату модификации файла неизменной. Это критически важно для аудита, отладки и работы с конфигурациями, где время изменения является ключевым параметром.

Поддержка отмены действий (Undo/Redo).

4. ⚙️ Системные утилиты
Проверка прав доступа: Индикатор текущего уровня привилегий пользователя (обычный пользователь/администратор).
Запрос прав администратора: Встроенная функция для перезапуска приложения с повышенными привилегиями, необходимыми для работы с системными файлами (например, hosts в C:\Windows\System32\).


🖥️ Технические характеристики
Язык программирования: Python 3.8+

Графический интерфейс (GUI): Построен на современном фреймворке CustomTkinter, обеспечивающем тёмную тему оформления и нативные элементы управления Windows.

Архитектура: Модульная, разделённая на логические компоненты (core, ui, widgets), что обеспечивает лёгкость поддержки и расширения.

Целевая платформа: Microsoft Windows 10/11. Для полной функциональности блокировщика трафика и работы с системными файлами требуется запуск с правами администратора.

Зависимости: Основная зависимость — библиотека customtkinter.
